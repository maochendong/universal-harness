# NOTES

## 已确认的教学偏好

- `teach-v2/` 与 `teach/` 1.x 并行保留并纳入 Git。
- 主线为架构优先；验证方法为证据优先。
- 首批交付完整课程地图、参考资料和前四节核心课。
- 每课使用源码实验强度：架构图、调用血缘、真实源码、可复跑命令、即时测验和决策练习。
- 目标设计与当前实现双轨呈现；Provider/Port 使用 `已连通`、`部分连通`、`未连通`，同时注明证据维度。
- 第五课以后根据回答、误区和实验结果自适应生成。

## 状态判定口径

- **已连通**：当前代码存在真实生产消费点，并有当前提交上的自动化或 Ledger 证据。
- **部分连通**：接口、Adapter 或生产路径存在，但当前外部 Provider、发布环境或完整闭环证据不足。
- **未连通**：只有目标设计或组件定义，当前生产调用链没有消费点。

状态必须带 `source_commit` 和证据范围；不得把历史真实调用自动升级为当前提交的外部复验。

## 学习状态口径

首页本地状态只分为“未浏览、已浏览、自报完成实验”，用于导航，不代表掌握。`learning-records/` 只有在用户通过回答、追踪或决策练习展示理解后才写入，文件名使用 `0001-<dash-case-name>.md`。

## 课程演进

首批四课固定建立共同语言：系统真相、CapabilityPlan、端到端追踪链、模型连通性。后续课程从 Ledger、Impact、治理、执行、完成判定、反馈、产品投影与发布中选择下一课，优先补足学习记录暴露的薄弱环节。
